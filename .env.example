# This application no longer requires environment variables
# All configuration is stored locally in browser localStorage

# The application connects directly to your Jira on-premise server
# Configuration is done through the UI setup process